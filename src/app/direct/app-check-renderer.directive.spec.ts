import { AppCheckRendererDirective } from './app-check-renderer.directive';

describe('AppCheckRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCheckRendererDirective();
    expect(directive).toBeTruthy();
  });
});
